# ethr-did-auto-config

Self-adjusting configuration for ethr-did-resolver.

This package will automatically configure the ethr-did-resolver using public RPC endpoints from chainlist.org for known
deployments of the ERC1056 registry.

